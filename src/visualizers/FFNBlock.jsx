/**
 * Placeholder for Phase 3. Before/after FFN bar chart.
 * Reads: store.modelOutput (FFN intermediate for current layer).
 */

export default function FFNBlock() {
  return (
    <div className="rounded border border-gray-200 bg-gray-50 p-4">
      <h3 className="text-sm font-semibold text-gray-700">FFN Block</h3>
      <p className="mt-2 text-xs text-gray-500">Run the model to see FFN.</p>
    </div>
  );
}
